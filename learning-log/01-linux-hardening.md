SSH key authentication will be stored in hardware and without the key we cant login to server, but password authentication is kinda not much secure than key authentication just like how cold wallet and hot wallet work in web3
permitrootlogin no - disable remote login as root user
ufw (uncomplicated firewall) will deny every connection on any port by default and only allow the connection which i permit
default deny incoming is a firewall which will block all connection from internet unless created a rule to allow them
unattended-upgrades helps update the linux os to new version automatically when new version or security patch available
